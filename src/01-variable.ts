let myVariableName: string = "Elon";
let netWorth: number;
let isLoading : boolean;

myVariableName = "Musk";
netWorth = 200;
isLoading = false;

const func = (a:number, b: number) : number => {
    return a + b;
}

let album: string | number;
// this is called union type, it can be either string or number
album = "Pop";
album = 100;

let isActive: number | boolean;

